spring:
    datasource:
        username: yada
        password: yada
        initialization-mode: always
        url: "jdbc:postgresql://localhost:5432/yada"
    jpa:
      show-sql: true
      database: POSTGRESQL